services:
  build:
    build:
      context: ./src
    volumes:
      - ./:/src
    environment:
      - BUILD_ONCE

  serve:
    image: caddy:2
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./web:/etc/caddy:ro
    environment:
      - SERVER_ADDRESS
      - SERVER_NAME
